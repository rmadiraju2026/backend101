import Image from 'next/image'
import styles from './page.module.css'

export default function Home() {
  return (
    <script>
      import { sql } from '@vercel/postgres';
      CREATE TABLE backend101 (
          Column1 int,
          Column2 varchar(255),
          Column3 varchar(255),
          Column4 varchar(255),
          Column5 varchar(255)
      );
    </script>
  )
}
